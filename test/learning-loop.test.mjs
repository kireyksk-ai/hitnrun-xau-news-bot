import assert from 'node:assert/strict';import test from 'node:test';import{mkdtempSync}from'node:fs';import{tmpdir}from'node:os';import{join}from'node:path';import{IntelligenceStore}from'../dist/intelligence-store.js';import{runLearningLoop}from'../dist/learning-loop.js';
test('runtime quant loop waits for samples then persists guarded daily model and relationships',()=>{const store=new IntelligenceStore(join(mkdtempSync(join(tmpdir(),'loop-')),'state.json'));for(let i=0;i<45;i++)store.recordMarketSnapshot({capturedAt:`2026-01-01T${String(i%24).padStart(2,'0')}:${String(i).padStart(2,'0')}:00Z`,session:'ASIA',values:{XAUUSD:{price:2000+i,changePercent:0,fresh:true},DXY:{price:100+i/10,changePercent:0,fresh:true},US10Y:{price:4+i/100,changePercent:0,fresh:true},WTI:{price:70+i/10,changePercent:0,fresh:true},VIX:{price:20+i/10,changePercent:0,fresh:true}}});runLearningLoop(store,'2026-01-01T23:59:00Z');assert.equal(store.quantitative().models.length,1);assert.equal(store.quantitative().relationships.length,4);assert.ok(store.quantitative().models[0].sampleSize>=40);runLearningLoop(store,'2026-01-01T23:59:30Z');assert.equal(store.quantitative().models.length,1);});
test('runtime drift is sample-guarded, bounded and restart-idempotent',()=>{const path=join(mkdtempSync(join(tmpdir(),'drift-')),'state.json'),store=new IntelligenceStore(path);for(let i=0;i<45;i++)store.recordMarketSnapshot({capturedAt:`2026-01-01T${String(i%24).padStart(2,'0')}:${String(i).padStart(2,'0')}:00Z`,session:'ASIA',values:{XAUUSD:{price:2000+i,changePercent:0,fresh:true},DXY:{price:100+i/10,changePercent:0,fresh:true},US10Y:{price:4+i/100,changePercent:0,fresh:true},WTI:{price:70+i/10,changePercent:0,fresh:true},VIX:{price:20+i/10,changePercent:0,fresh:true}}});runLearningLoop(store,'2026-01-01T23:59:00Z');const resumed=new IntelligenceStore(path);runLearningLoop(resumed,'2026-01-02T23:59:00Z');assert.equal(new IntelligenceStore(path).quantitative().drift.length,4);runLearningLoop(new IntelligenceStore(path),'2026-01-02T23:59:30Z');assert.equal(new IntelligenceStore(path).quantitative().drift.length,4);});
