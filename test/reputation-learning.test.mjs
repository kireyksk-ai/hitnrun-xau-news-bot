import assert from 'node:assert/strict';import test from 'node:test';import{mkdtempSync}from'node:fs';import{tmpdir}from'node:os';import{join}from'node:path';import{IntelligenceStore}from'../dist/intelligence-store.js';import{learnReputation}from'../dist/reputation-learning.js';
test('reputation is restart-safe, separate and sample guarded',()=>{const path=join(mkdtempSync(join(tmpdir(),'rep-')),'state.json'),s=new IntelligenceStore(path);s.recordExperience({id:'checkpoint:x',createdAt:'2026-01-01',regime:'R',trigger:'event',attribution:'POSSIBLE_DRIVER',confidence:0,outcome:'MARKET_CONFIRMATION',checkpoint:{id:'x',eventId:'e',createdAt:'2026-01-01',scheduledAt:'2026-01-01',horizon:'INTRADAY',status:'COMPLETED',attempts:1}});learnReputation(s,'checkpoint:x','Reuters','Fed');learnReputation(new IntelligenceStore(path),'checkpoint:x','Reuters','Fed');const q=new IntelligenceStore(path).quantitative();assert.equal(q.sourceEvidence.Reuters.observations,1);assert.equal(q.sourceEvidence.Reuters.state,'INSUFFICIENT_SAMPLE');assert.equal(q.actorEvidence.Fed.observations,1);});
